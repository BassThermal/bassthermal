import { mkdir, readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
import vm from 'node:vm';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const ROOT = path.resolve(__dirname, '..');
const PUBLIC_DIR = path.join(ROOT, 'public');
const MANIFEST_FILE = path.join(PUBLIC_DIR, 'store-assets.generated.js');
const MAX_SCREENSHOTS = 10;
const REQUEST_DELAY_MS = 140;

const APPS = [
  { slug: 'dualticker', name: 'DualTicker', windowsProductId: '9p4txws57pld', androidPackage: 'com.bassthermal.dualtickerlens', webUrl: 'https://www.dualticker.com/' },
  { slug: 'retrofy', name: 'RetroFy', windowsProductId: '9nk0mhg9f29r', androidPackage: null, androidDiscoveryName: 'Retrofy' },
  { slug: 'coptic-dictionary', name: 'Coptic Dictionary', windowsProductId: '9nxmkjl625r2', androidPackage: 'com.bassthermal.copticdictionary' },
  { slug: 'icon-pack-builder', name: 'Icon Pack Builder', windowsProductId: '9mxvt3dfq295' },
  { slug: 'favicon-harvester', name: 'Favicon Harvester', windowsProductId: '9mxj31fxcq4f' },
  { slug: 'isbn-manager', name: 'ISBN Manager', windowsProductId: '9nr0nblx10fb' },
  { slug: 'rss-finder', name: 'RSS Finder', lab: true }
];

const TEXT_HEADERS = {
  'user-agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0 Safari/537.36 BassThermalAssetHarvester/2.1',
  'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,application/json;q=0.8,*/*;q=0.7',
  'accept-language': 'en-US,en;q=0.9'
};
const IMG_HEADERS = { 'user-agent': TEXT_HEADERS['user-agent'], 'accept': 'image/avif,image/webp,image/apng,image/svg+xml,image/*,*/*;q=0.8', 'accept-language': 'en-US,en;q=0.9' };
const arr = (v) => Array.isArray(v) ? v.filter(Boolean) : [];
const sleep = (ms = REQUEST_DELAY_MS) => new Promise((resolve) => setTimeout(resolve, ms));

function decodeHtmlAndEscapes(input) { return String(input || '').replace(/\\u0026/g, '&').replace(/\\u003d/g, '=').replace(/\\u003a/g, ':').replace(/\\u002f/gi, '/').replace(/\\\//g, '/').replace(/\u0026/g, '&').replace(/\u003d/g, '=').replace(/\u003a/g, ':').replace(/\u002f/gi, '/').replace(/&amp;/g, '&').replace(/&quot;/g, '"').replace(/&#39;/g, "'").replace(/&lt;/g, '<').replace(/&gt;/g, '>'); }
function cleanUrl(url) { return decodeHtmlAndEscapes(url).trim().replace(/^[('\"\s]+/g, '').replace(/[)'\",;<>\]\s\\]+$/g, '').replace(/\\/g, ''); }
function uniq(values) { return [...new Set((values || []).map(cleanUrl).filter(Boolean))]; }
function safeName(value) { return String(value || '').toLowerCase().replace(/[^a-z0-9._-]+/g, '-').replace(/^-+|-+$/g, '') || 'asset'; }
function extFrom(url, contentType) { const ct = String(contentType || '').toLowerCase(); if (ct.includes('image/webp')) return '.webp'; if (ct.includes('image/png')) return '.png'; if (ct.includes('image/jpeg') || ct.includes('image/jpg')) return '.jpg'; if (ct.includes('image/svg')) return '.svg'; const m = String(url || '').match(/\.(webp|png|jpe?g|svg)(?:$|[?#&])/i); return m ? (m[1].toLowerCase() === 'jpeg' ? '.jpg' : `.${m[1].toLowerCase()}`) : '.img'; }
async function fetchText(url, headers = TEXT_HEADERS) { await sleep(); const r = await fetch(url, { headers, redirect: 'follow' }); if (!r.ok) throw new Error(`HTTP ${r.status}`); return r.text(); }
async function fetchJson(url, headers = TEXT_HEADERS) { await sleep(); const r = await fetch(url, { headers: { ...headers, accept: 'application/json,*/*;q=0.8' }, redirect: 'follow' }); if (!r.ok) throw new Error(`HTTP ${r.status}`); return r.json(); }
async function fetchImage(url) { await sleep(); const r = await fetch(url, { headers: IMG_HEADERS, redirect: 'follow' }); if (!r.ok) throw new Error(`HTTP ${r.status}`); const buffer = Buffer.from(await r.arrayBuffer()); const contentType = r.headers.get('content-type') || ''; return { buffer, contentType, ext: extFrom(url, contentType), dimensions: readImageDimensions(buffer) }; }

function readImageDimensions(buffer) { try { if (buffer.length >= 24 && buffer.toString('ascii', 1, 4) === 'PNG') return { width: buffer.readUInt32BE(16), height: buffer.readUInt32BE(20) }; if (buffer.length >= 10 && buffer[0] === 0xff && buffer[1] === 0xd8) { let o = 2; while (o < buffer.length) { if (buffer[o] !== 0xff) break; const marker = buffer[o + 1]; const len = buffer.readUInt16BE(o + 2); if ([0xc0,0xc1,0xc2,0xc3,0xc5,0xc6,0xc7,0xc9,0xca,0xcb,0xcd,0xce,0xcf].includes(marker)) return { height: buffer.readUInt16BE(o + 5), width: buffer.readUInt16BE(o + 7) }; o += 2 + len; } } if (buffer.length >= 30 && buffer.toString('ascii', 0, 4) === 'RIFF' && buffer.toString('ascii', 8, 12) === 'WEBP') { const t = buffer.toString('ascii', 12, 16); if (t === 'VP8X') return { width: 1 + buffer.readUIntLE(24, 3), height: 1 + buffer.readUIntLE(27, 3) }; if (t === 'VP8 ') return { width: buffer.readUInt16LE(26) & 0x3fff, height: buffer.readUInt16LE(28) & 0x3fff }; } } catch {} return { width: 0, height: 0 }; }
function classifyImage(url, dimensions, hint = '') { const w = dimensions?.width || 0, h = dimensions?.height || 0, ratio = w && h ? w / h : 0, hay = `${url} ${hint}`.toLowerCase(); const icon = /logo|icon|tile|square|packageicon|appicon|iconic|storelogo/.test(hay) || (w >= 80 && h >= 80 && ratio > 0.72 && ratio < 1.38 && w <= 1200 && h <= 1200); const shot = /screenshot|screen|hero|poster|background|trailer|imagegallery/.test(hay) || (w >= 480 && h >= 260 && ratio >= 1.25); if (shot && !/logo|icon|tile|storelogo/.test(hay)) return 'screenshot'; if (icon) return 'icon'; if (shot) return 'screenshot'; return 'other'; }
async function saveImage(app, platform, stem, url, image) { const rel = `/assets/apps/${safeName(app.slug)}/${safeName(platform)}/${safeName(stem)}${image.ext || '.img'}`; const full = path.join(PUBLIC_DIR, rel); await mkdir(path.dirname(full), { recursive: true }); await writeFile(full, image.buffer); return rel; }
function appSeedRecord() { return { icon: { fallback: null, android: null, windows: null, web: null }, screenshots: { android: [], windows: [], web: [] } }; }
function cloneRecord(record) { const base = appSeedRecord(), src = record || {}; return { icon: { ...base.icon, ...(src.icon || {}) }, screenshots: { android: arr(src.screenshots?.android), windows: arr(src.screenshots?.windows), web: arr(src.screenshots?.web) } }; }
async function readExistingManifest() { try { const content = await readFile(MANIFEST_FILE, 'utf8'); const sandbox = { window: {}, document: { querySelector: () => null, createElement: () => ({}), head: { appendChild: () => {} } }, setTimeout: () => 0, clearTimeout: () => {}, setInterval: () => 0, clearInterval: () => {}, console: { log: () => {}, warn: () => {}, error: () => {} } }; try { vm.runInNewContext(content, sandbox, { timeout: 500 }); } catch {} return sandbox.window.BT_STORE_ASSETS || { apps: {} }; } catch { return { apps: {} }; } }
function collectUrlsFromText(text, source = '') { const decoded = decodeHtmlAndEscapes(text); const out = []; for (const re of [/https?:\/\/[^"'<>\s\\]+/g, /https%3A%2F%2F[^"'<>\s\\]+/gi]) for (const raw of decoded.match(re) || []) out.push({ url: cleanUrl(raw.replace(/%3A/gi, ':').replace(/%2F/gi, '/').replace(/%3F/gi, '?').replace(/%26/gi, '&').replace(/%3D/gi, '=')), hint: source }); return out; }
function collectMicrosoftImageCandidates(value, out = [], hint = '') { if (value == null) return out; if (typeof value === 'string') { for (const item of collectUrlsFromText(value, hint)) if (/store-images|store-images\.s-microsoft|store-images\.microsoft|storeedge|microsoft\.com\/.*image/i.test(item.url)) out.push(item); return out; } if (Array.isArray(value)) { value.forEach((item, i) => collectMicrosoftImageCandidates(item, out, `${hint} array[${i}]`)); return out; } if (typeof value === 'object') for (const [key, item] of Object.entries(value)) collectMicrosoftImageCandidates(item, out, `${hint} ${key}`); return out; }
function filterMicrosoftCandidates(candidates) { const filtered = [], seen = new Set(); for (const c of candidates) { const url = cleanUrl(c.url); if (!/^https?:\/\//i.test(url)) continue; if (!/store-images|store-images\.s-microsoft|store-images\.microsoft|microsoft/i.test(url)) continue; if (/badge|rating|age|qr|barcode|glyph|sprite|favicon/i.test(url)) continue; const key = url.replace(/([?&](w|h|width|height|mode|format|quality)=[^&]+)/gi, ''); if (seen.has(key)) continue; seen.add(key); filtered.push({ url, hint: c.hint || '' }); } return filtered; }
function microsoftPageUrls(productId) { const id = productId.toLowerCase(), markets = [{gl:'CA',hl:'en-US'},{gl:'US',hl:'en-US'},{gl:'CA',hl:'en-CA'}], urls = []; for (const m of markets) { urls.push(`https://apps.microsoft.com/detail/${id}?hl=${m.hl}&gl=${m.gl}`); urls.push(`https://apps.microsoft.com/store/detail/${id}?hl=${m.hl}&gl=${m.gl}`); urls.push(`https://apps.microsoft.com/detail/${id}?hl=${m.hl}&gl=${m.gl}&ocid=pdpshare`); } return urls; }
function microsoftJsonUrls(productId) { const id = productId.toUpperCase(), urls = []; for (const [market, lang] of [['CA','en-US'],['US','en-US'],['CA','en-CA'],['GB','en-GB']]) { urls.push(`https://displaycatalog.mp.microsoft.com/v7.0/products?bigIds=${id}&market=${market}&languages=${lang}&MS-CV=DGU1mcuYo0WMMp+F.1`); urls.push(`https://storeedgefd.dsx.mp.microsoft.com/v9.0/products/${id}?market=${market}&locale=${lang}&deviceFamily=Windows.Desktop`); urls.push(`https://storeedgefd.dsx.mp.microsoft.com/v9.0/products/${id}?market=${market}&locale=${lang}`); } return urls; }
async function harvestMicrosoft(app, report) { if (!app.windowsProductId) return { icon: null, shots: [] }; const candidates = [], pageFailures = [], jsonFailures = []; for (const url of microsoftPageUrls(app.windowsProductId)) { try { const html = await fetchText(url); candidates.push(...collectMicrosoftImageCandidates(html, [], `html ${url}`)); const scriptUrls = uniq((html.match(/<script[^>]+src=["']([^"']+)/gi) || []).map((tag) => { const m = tag.match(/src=["']([^"']+)/i); if (!m) return ''; try { return new URL(decodeHtmlAndEscapes(m[1]), url).href; } catch { return ''; } })).filter((src) => /apps\.microsoft|store|assets|_next|static/i.test(src)).slice(0, 12); for (const scriptUrl of scriptUrls) { try { const script = await fetchText(scriptUrl, { ...TEXT_HEADERS, accept: 'application/javascript,text/javascript,*/*;q=0.8' }); candidates.push(...collectMicrosoftImageCandidates(script, [], `script ${scriptUrl}`)); } catch {} } } catch (e) { pageFailures.push(`${url} ${e.message}`); } } for (const url of microsoftJsonUrls(app.windowsProductId)) { try { candidates.push(...collectMicrosoftImageCandidates(await fetchJson(url), [], `json ${url}`)); } catch (e) { jsonFailures.push(`${url} ${e.message}`); } } const urls = filterMicrosoftCandidates(candidates); if (!urls.length) { report.push(`! ${app.slug} windows no Microsoft image URLs found (${pageFailures.length} page failures, ${jsonFailures.length} catalog failures)`); return { icon: null, shots: [] }; } let icon = null; const shots = []; let inspected = 0; for (const c of urls.slice(0, 48)) { if (shots.length >= MAX_SCREENSHOTS && icon) break; inspected++; try { const image = await fetchImage(c.url), type = classifyImage(c.url, image.dimensions, c.hint); if (!icon && (type === 'icon' || /logo|icon|tile|storelogo/i.test(c.hint))) { icon = await saveImage(app, 'windows', 'icon', c.url, image); continue; } if (shots.length < MAX_SCREENSHOTS && type !== 'icon') shots.push(await saveImage(app, 'windows', `shot-${String(shots.length + 1).padStart(2, '0')}`, c.url, image)); } catch {} } if (!icon) for (const c of urls.slice(0, 20)) { try { icon = await saveImage(app, 'windows', 'icon', c.url, await fetchImage(c.url)); break; } catch {} } report.push(`✓ ${app.slug} windows ${icon ? 'icon' : 'no-icon'} + ${shots.length} screenshots (${urls.length} candidates, ${inspected} inspected)`); return { icon, shots }; }
async function resolveAndroidPackage(app) { if (app.androidPackage) return app.androidPackage; try { const html = decodeHtmlAndEscapes(await fetchText('https://play.google.com/store/apps/developer?id=BassThermal&hl=en_CA&gl=CA')); for (const m of html.matchAll(/\/store\/apps\/details\?id=([a-zA-Z0-9._]+)/g)) { const around = html.slice(Math.max(0, m.index - 260), m.index + 320).toLowerCase(); if (around.includes((app.androidDiscoveryName || app.name).toLowerCase())) return m[1]; } } catch {} return null; }
async function harvestAndroid(app, report) { const pkg = await resolveAndroidPackage(app); if (!pkg) { report.push(`! ${app.slug} android unresolved package id`); return { icon: null, shots: [] }; } try { const html = decodeHtmlAndEscapes(await fetchText(`https://play.google.com/store/apps/details?id=${pkg}&hl=en_CA&gl=CA`)); const urls = uniq((html.match(/https:\/\/play-lh\.googleusercontent\.com\/[^"'<>\\\s)]+/g) || []).map(cleanUrl)); let icon = null; const shots = []; for (const url of urls) { try { const image = await fetchImage(url), type = classifyImage(url, image.dimensions, 'google play'); if (!icon && type === 'icon') { icon = await saveImage(app, 'android', 'icon', url, image); continue; } if (shots.length < MAX_SCREENSHOTS && type !== 'icon') shots.push(await saveImage(app, 'android', `shot-${String(shots.length + 1).padStart(2, '0')}`, url, image)); } catch {} } if (!icon && urls[0]) try { icon = await saveImage(app, 'android', 'icon', urls[0], await fetchImage(urls[0])); } catch {} report.push(`✓ ${app.slug} android ${icon ? 'icon' : 'no-icon'} + ${shots.length} screenshots`); return { icon, shots }; } catch (e) { report.push(`! ${app.slug} android ${e.message}`); return { icon: null, shots: [] }; } }
async function harvestWeb(app, report) { if (!app.webUrl) return { shots: [] }; try { const html = await fetchText(app.webUrl, { ...TEXT_HEADERS, accept: 'text/html,*/*;q=0.8' }); const m = html.match(/<meta[^>]+property=["']og:image["'][^>]+content=["']([^"']+)/i) || html.match(/<meta[^>]+content=["']([^"']+)["'][^>]+property=["']og:image/i); if (!m?.[1]) throw new Error('og:image missing'); const url = new URL(decodeHtmlAndEscapes(m[1]), app.webUrl).href; const shot = await saveImage(app, 'web', 'shot-01', url, await fetchImage(url)); report.push(`✓ ${app.slug} web 1 screenshot`); return { shots: [shot] }; } catch (e) { report.push(`! ${app.slug} web ${e.message}`); return { shots: [] }; } }
function mergePlatform(existing, harvested, platform) { const record = cloneRecord(existing); if (harvested.icon) record.icon[platform] = harvested.icon; if (harvested.shots?.length) record.screenshots[platform] = harvested.shots; record.icon.fallback = record.icon.android || record.icon.windows || record.icon.web || record.icon.fallback || null; return record; }
function previewLoaderSnippet() { return `\n(() => {\n  if (document.querySelector('script[data-bt-asset-preview]')) return;\n  const script = document.createElement('script');\n  script.src = '/asset-preview.js?v=stable-v4';\n  script.defer = true;\n  script.dataset.btAssetPreview = '1';\n  document.head.appendChild(script);\n})();\n`; }
async function main() { const existing = await readExistingManifest(); const manifest = { generatedAt: new Date().toISOString(), source: 'tools/harvest-store-assets.mjs robust-ms-v2.1', apps: { ...(existing.apps || {}) } }; const report = []; console.log('BassThermal asset harvest'); for (const app of APPS) { if (app.lab) { manifest.apps[app.slug] = cloneRecord(manifest.apps[app.slug]); report.push(`- ${app.slug} lab/no store assets`); continue; } let record = cloneRecord(manifest.apps[app.slug]); record = mergePlatform(record, await harvestAndroid(app, report), 'android'); record = mergePlatform(record, await harvestMicrosoft(app, report), 'windows'); const web = await harvestWeb(app, report); if (web.shots?.length) record.screenshots.web = web.shots; record.icon.fallback = record.icon.android || record.icon.windows || record.icon.web || record.icon.fallback || null; manifest.apps[app.slug] = record; } await writeFile(MANIFEST_FILE, `window.BT_STORE_ASSETS = ${JSON.stringify(manifest, null, 2)};\n${previewLoaderSnippet()}`, 'utf8'); report.forEach((line) => console.log(line)); console.log('wrote public/store-assets.generated.js'); }
main().catch((error) => { console.error(error); process.exitCode = 1; });
