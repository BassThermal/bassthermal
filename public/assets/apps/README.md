# BassThermal app assets

Generated by:

```bash
node tools/harvest-store-assets.mjs
```

The generated manifest is:

`public/store-assets.generated.js`

The website must still load if assets are missing; missing assets must never show broken images.
