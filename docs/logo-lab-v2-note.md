# Logo Lab v2 correction

This branch replaces the merged command-driven Logo Lab v1 with a live visual control panel.

The file exists only to force a fresh branch comparison after PR #103 was merged before the v2 commits landed. It should be removed before merge if GitHub no longer requires it.
