import worker from './worker-v2.js';

// Public HTML is generated with its permanent brand shell before deployment.
// The Worker owns redirects, Visits APIs, and server-side behavior only.
export default worker;
