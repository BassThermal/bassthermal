{
  "name": "bassthermal",
  "private": true,
  "scripts": {
    "deploy": "npm run assets:index && wrangler deploy",
    "dev": "npm run assets:index && wrangler dev",
    "assets:index": "node tools/build-asset-manifest.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.16.0"
  }
}
